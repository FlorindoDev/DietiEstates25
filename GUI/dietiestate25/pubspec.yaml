name: dietiestate25
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  
  http: ^1.3.0  # Controlla se esiste una versione più recente


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - images/
    - images/logo/
    - assets/
  
  fonts:
    - family: Goldbill-XLBold
      fonts:
        - asset: fonts/Goldbill-XLBold.ttf